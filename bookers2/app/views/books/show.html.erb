
  <main>
    <p>you have created book successfully</p>
    <div class="container">
      <div class="row">
        <div class="col-md-3">
          <h2>User info</h2>
          <img class="attachment user profile_image fallback" src="no_image.jpg" >
          <table>
            <tbody>
              <tr>
                <th>name</th>
                <th>@book.user.name</th>
              </tr>
              <tr>
                <th>intoduction</th>
                <th>@book.user.introduction</th>
              </tr>
            </tbody>
          </table>
          <div class="row"><%= link_to user_edit_path(@user)%>
          </div>
          <h2 class="mt-3">New Book</h2>
          <% form_with model :@book local:true do |f| %>
           <%= f.text_field :title %>
           <%= f.text_area :opinion %>
           <%= f.submit "Create Book"%>
        </div>
        <div class="col-md-8 offset-md-1">
          <h2>Book detail</h2>
          <table class="table">
            <tbody><% @book.each %>
              <tr>
                <td><% link_to マイページ %>do
                @user.profile_image @user.name
                  end</td>
                <td><%= title%></td>
                <td><%= opinion %></td>
                <td><% link_to "edit",book_edit_path(@user) %></td>
                <td><% link_to "Destroy",book_destory_path(@user),method: :delete %></td>
            <% end %>    
              </tr>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </main>

